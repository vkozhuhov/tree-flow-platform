# Design.App - Фреймворк для создания .NET приложений

---

## 🚀 Основы

### Что такое Design.App?

Design.App — это "конструктор" для .NET приложений. Вместо того чтобы писать много кода для настройки логирования, конфигурации, веб-сервера и т.п., вы просто "собираете" приложение из готовых блоков.

**Основные концепции:**
- 🧱 **Модули (Tiles)** — готовые блоки функциональности
- 🔧 **Fluent API** — цепочка методов для настройки
- 💉 **Dependency Injection** — автоматическая передача компонентов туда, где они нужны

### Быстрый старт

#### 1. Минимальное CLI приложение

**Что делает:** Простое приложение, которое выводит сообщение в консоль и завершается.
```csharp
using MCDis256.Design.App;
using MCDis256.Design.App.Interface.Log.Toolkit;
using MCDis256.Design.App.Modules.Log;

public class Program
{
  static Task<int> Main() =>
    AppHome.New()                    // 1. Создаем новое приложение
      .UseConsoleLogListener()      // 2. Добавляем логирование в консоль
      .OnStart(ctx =>               // 3. Что делать при запуске
        ctx.Log.Info("Привет, мир!"))  // 4. Выводим сообщение
      .RunAsync();                  // 5. Запускаем приложение
}
```

**Пошаговое объяснение:**
1. `AppHome.New()` — создает новый экземпляр приложения
2. `UseConsoleLogListener()` — добавляет возможность логирования в консоль
3. `OnStart()` — задает действие, которое выполнится при запуске
4. `ctx.Log.Info()` — выводит информационное сообщение
5. `RunAsync()` — запускает приложение и ждет завершения

#### 2. Минимальный веб-сервер

**Что делает:** Создает веб-сервер, который отвечает "Hello World!" на запросы.
```csharp
using System.Net;
using MCDis256.Design.App;
using MCDis256.Design.App.Web;  // ⬅️ Обязательно для UseWebServer
using Microsoft.AspNetCore.Builder;

public class Program
{
  static Task Main() =>
    AppHome.New()                    // 1. Создаем приложение
      .UseConsoleLogListener()      // 2. Добавляем логирование
      .UseWebServer(builder =>      // 3. Добавляем веб-сервер
      {
        builder
          .AddEndpoint(IPAddress.Loopback, 5000)  // 4. Слушаем порт 5000
          .Setup(app => app.MapGet("/", () => "Hello World!"));  // 5. Настраиваем маршрут
      })
      .RunAsync();                  // 6. Запускаем
}
```

**Пошаговое объяснение:**
1. `AppHome.New()` — создаем приложение
2. `UseConsoleLogListener()` — логирование в консоль
3. `UseWebServer()` — добавляем веб-сервер
4. `AddEndpoint()` — указываем адрес и порт (localhost:5000)
5. `MapGet("/", ...)` — при запросе к корню "/" возвращаем "Hello World!"
6. `RunAsync()` — запускаем сервер

**Запуск:** `dotnet run` и откройте `http://localhost:5000` в браузере.

#### 3. CLI с парсингом аргументов

**Что делает:** Приложение, которое принимает имя пользователя как параметр командной строки.
```csharp
using CommandLine;
using MCDis256.Design.App;
using MCDis256.Design.App.Cli;  // ⬅️ Обязательно для RunCliAsync
using MCDis256.Design.App.Modules.Log;

// Класс для хранения параметров командной строки
public class Options
{
  [Option('n', "name", Required = true)]  // Обязательный параметр --name
  public string Name { get; set; }
}

public class Program
{
  static Task<int> Main() =>
    AppHome.New()                    // 1. Создаем приложение
      .UseConsoleLogListener()      // 2. Добавляем логирование
      .RunCliAsync<Options>(        // 3. Запускаем CLI с парсингом аргументов
        async (opts, ctx) =>      // 4. Что делать при успешном парсинге
        {
          ctx.Log.Info($"Привет, {opts.Name}!");  // 5. Выводим приветствие
          return 0;             // 6. Возвращаем код успеха
        },
        ctx => 1                  // 7. Что делать при ошибке парсинга
      );
}
```

**Пошаговое объяснение:**
1. `Options` — класс для хранения параметров командной строки
2. `[Option]` — атрибут, указывающий как парсить параметр
3. `RunCliAsync<Options>()` — запускает CLI с автоматическим парсингом
4. Первая функция — выполняется при успешном парсинге
5. Вторая функция — выполняется при ошибке парсинга

**Запуск:** `dotnet run -- --name "Иван"`

---

## 📚 Структура решения

| Проект | Описание |
|--------|----------|
| **App** | Ядро фреймворка с базовыми интерфейсами |
| **App.Cli** | Поддержка CLI приложений |
| **App.Conf** | Система конфигурации (JSON файлы, hot-reload) |
| **App.Logging** | Логирование (консоль, JSON, файлы) |
| **App.Web** | Интеграция с ASP.NET Core |
| **App.CrashReporter** | Обработка сбоев |

---

## 🔥 Расширенные возможности

> **Примечание:** Design.App использует **Reactive Extensions (Rx)** для реактивного программирования. Многие концепции построены на потоках данных и автоматическом обновлении состояния.

### Конфигурация с hot-reload
```csharp
[AppConfig(Name = "config.json")]
public class MyConfig
{
  public string Setting { get; set; }
}

// В приложении
.UseConfFile<MyConfig>()
.OnStartAsync(async ctx =>
{
  var config = ctx.Locate<IAppConfigProvider>()
    .Resolve<MyConfig>().Value?.Value;
  // Конфигурация автоматически обновляется при помощи Rx-цепочек
})
```

### Dependency Injection
```csharp
public interface IMyService { }
internal class MyService : IMyService, IAppComponent<IMyService>
{
  public static IMyService Activate(IAppContext ctx) =>
    ctx.Activate((ILog log) => new MyService(log));
}

// Регистрация в приложении
.UseExport<IMyService, MyService>()
```

### Веб-сервер с endpoints
```csharp
.UseWebServer(builder =>
{
  builder
    .AddEndpoint(IPAddress.Any, 5000)
    .Setup(app =>
    {
      app.MapGet("/api/data", () => new { message = "Hello" });
    });
})
```

---

## 🧩 Модули AppHome и расширения

### Что такое модули (Tiles)?

**Модули (Tiles)** — это строительные блоки приложения в Design.App. Каждый модуль представляет собой функциональный компонент, который можно подключить к приложению через метод `Use()`.

**Жизненный цикл модуля:**

1. **Configure** — настройка модуля (регистрация зависимостей)
2. **Export** — экспорт зависимостей в контейнер DI
3. **Wire** — связывание компонентов после экспорта
4. **PostWire** — финальная настройка после связывания
5. **PreStart** — подготовка перед запуском
6. **OnStarted** — выполнение после успешного запуска

### Типы модулей

#### 1. Базовые модули (из пакета App)

| Модуль | Метод | Описание |
|--------|-------|----------|
| **LogAppInfoTile** | `UseLogAppInfo()` | Выводит информацию о приложении при старте |
| **SigTermHandlerTile** | `UseHandleProcessExit()` | Обрабатывает сигналы завершения (Ctrl+C, SIGTERM) |

#### 2. Модули конфигурации (из пакета App.Conf)

| Модуль | Метод | Описание |
|--------|-------|----------|
| **AppConfigProvider** | `UseAppConfigDirectory()` | Включает наблюдение за файлами конфигураций в папке `conf/` |
| **AppUserConfigProvider** | `UseAppUserConfigProvider()` | Провайдер для пользовательских конфигураций (AppData, UserProfile) |

#### 3. Модули логирования (из пакета App.Logging)

| Модуль | Метод | Описание |
|--------|-------|----------|
| **HourFileLogListener** | `UseHourFileLog()` | Логирование в файлы с разбивкой по часам |
| **StdoutJsonLogListener** | `UseStdoutJsonLog()` | Логирование в stdout в формате JSON |

#### 4. Модули веб-сервера (из пакета App.Web)

| Модуль | Метод | Описание |
|--------|-------|----------|
| **WebServerImpl** | `UseWebServer()` | Интеграция с ASP.NET Core |

#### 5. Модули CLI (из пакета App.Cli)

| Модуль | Метод | Описание |
|--------|-------|----------|
| **CLI Parser** | `RunCliAsync<T>()` | Парсинг аргументов командной строки |

### Базовые расширения для построения приложения

#### Dependency Injection (DI)

**Регистрация компонентов:**

```csharp
// 1. Регистрация компонента с автоматической активацией
.UseExport<IMyService, MyService>()

// 2. Регистрация через фабрику
.UseExportFactory<IMyService>(ctx => new MyService(ctx.Locate<ILog>()))

// 3. Регистрация готового экземпляра
.UseExportInstance<IMyService>(new MyService())

// 4. Регистрация через интерфейс IAppComponent
public interface IMyService { }
internal class MyService : IMyService, IAppComponent<IMyService>
{
  public static IMyService Activate(IAppContext ctx) =>
    ctx.Activate((ILog log) => new MyService(log));
}
```

**Использование компонентов:**

```csharp
// В контексте приложения
.OnStart(ctx =>
{
  var service = ctx.Locate<IMyService>();
  service.DoSomething();
})

// Активация с автоматическим разрешением зависимостей
.ActivateOnStart<IMyService>(service => service.Start())
```

#### Жизненный цикл приложения

```csharp
AppHome.New()
  // 1. Конфигурация (до создания контекста)
  .OnConfigure(cfg =>
  {
    cfg.Dependencies.ExportInstance<IMyService>(new MyService());
  })

  // 2. Связывание (после создания контекста)
  .OnWire(ctx =>
  {
    var service = ctx.Locate<IMyService>();
    service.Initialize();
  })

  // 3. Запуск
  .OnStart(ctx =>
  {
    ctx.Log.Info("Приложение запущено");
  })

  .RunAsync();
```

#### Условная конфигурация

```csharp
// Условное подключение модуля
.UseIf(condition, home => home.UseLogAppInfo())

// Условное подключение на основе переменной окружения
.UseIfEnv("ENABLE_LOGGING", home => home.UseLogAppInfo())

// Условное подключение с альтернативой
.UseIfElse(condition,
  home => home.UseLogAppInfo(),
  home => home.UseStdoutJsonLog())
```

### Разница между DI AppHome и ASP.NET Core DI

| Аспект | AppHome DI | ASP.NET Core DI |
|--------|------------|-----------------|
| **Контейнер** | Собственный контейнер (`AppDependencies`) | `IServiceCollection` / `IServiceProvider` |
| **Жизненный цикл** | Все зависимости — синглтоны (Lazy) | Transient, Scoped, Singleton |
| **Регистрация** | Через `ExportInstance`, `ExportFactory` | Через `AddTransient`, `AddScoped`, `AddSingleton` |
| **Разрешение** | `ctx.Locate<T>()` | Через конструктор или `IServiceProvider` |
| **Интеграция** | AppHome DI → ASP.NET через `UseDependency()` | ASP.NET DI используется в контроллерах |

**Интеграция AppHome DI с ASP.NET:**

```csharp
.UseWebServer(builder =>
{
  builder
    .UseDependency<IMyService>()  // Регистрирует IMyService из AppHome в ASP.NET DI
    .AddEndpoint(IPAddress.Any, 5000)
    .Setup(app =>
    {
      app.MapGet("/api", (IMyService service) =>
      {
        // service автоматически резолвится из AppHome контейнера
        return service.GetData();
      });
    });
})
```

**Важно:**
- AppHome DI создается **до** запуска ASP.NET Core
- Компоненты из AppHome доступны в ASP.NET контроллерах через `UseDependency()`
- AppHome DI использует **Lazy-инициализацию** для всех зависимостей
- Все зависимости в AppHome — **синглтоны** на время жизни приложения

### Работа с конфигурациями

#### 1. Конфигурация из файлов (App.Conf)

**Базовое использование:**

```csharp
// 1. Включаем наблюдение за папкой conf/
.UseAppConfigDirectory()

// 2. Загружаем конфигурацию из файла
.UseConfFile<MyConfig>()

// 3. Используем конфигурацию
.OnStart(ctx =>
{
  var config = ctx.Locate<MyConfig>();
  ctx.Log.Info($"City: {config.City}");
})
```

**Определение конфигурации:**

```csharp
[AppConfig(Name = "my-config.json")]  // Имя файла (опционально)
public class MyConfig
{
  public string City { get; set; } = "";
  public int UpdateIntervalSec { get; set; } = 10;
}
```

**Hot-reload конфигурации:**

```csharp
// Конфигурация автоматически обновляется при изменении файла
confProvider.Resolve<MyConfig>()
  .AssignIndex()
  .HotAlive(lifetime, (indexedCfg, life) =>
  {
    if (indexedCfg.Value?.Status == AppConfigStatus.Ok)
    {
      var config = indexedCfg.Value.Value;
      // Используем обновленную конфигурацию
    }
  });
```

**Кастомная папка конфигураций:**

```csharp
.UseAppConfigDirectory(DirectoryPath.FromCurrentDirectory()["custom-conf"])
```

#### 2. Конфигурация из переменных окружения

**Строковые переменные:**

```csharp
.UseAppEnvConfig<MyConfig>(ctx =>
{
  return new MyConfig
  {
    City = ctx.AsString("CITY"),           // Обязательная переменная
    ApiKey = ctx.AsStringOptional("API_KEY") ?? "default"  // Опциональная
  };
})
```

**JSON из переменных окружения:**

```csharp
// Переменная окружения содержит JSON
// export CONFIG='{"City":"Moscow","UpdateIntervalSec":10}'
.UseAppEnvJsonConfig<MyConfig>("CONFIG")

// С обработкой ошибок (возвращает default при ошибке)
.UseAppEnvJsonConfigOrDefault<MyConfig>("CONFIG")
```

**Доступные методы `ApplicationEnvContext`:**

| Метод | Описание |
|-------|----------|
| `AsString(string)` | Обязательная строковая переменная |
| `AsStringOptional(string)` | Опциональная строковая переменная |
| `AsUri(string)` | URI из переменной окружения |
| `AsUriOptional(string)` | Опциональный URI |
| `AsJson<T>(string)` | JSON из переменной окружения |

#### 3. Пользовательские конфигурации (AppUserConfigProvider)

**Настройка провайдера:**

```csharp
.UseAppUserConfigProvider(new AppUserConfigProviderOptions
{
  // Текущая директория
  CurrentDirectoryTransform = dir => dir["user-config"],

  // AppData (Windows: %APPDATA%, Linux: ~/.config)
  AppDataDirectoryTransform = dir => dir["MyApp"]["config"],

  // UserProfile (Windows: %USERPROFILE%, Linux: ~)
  UserProfileDirectoryTransform = dir => dir[".myapp"],

  // Опции сериализации (опционально)
  Serializer = new JsonSerializerOptions { ... }
})
```

**Использование:**

```csharp
var userConfigProvider = ctx.Locate<IAppUserConfigProvider>();

// Чтение (поиск в порядке: Current → AppData → UserProfile)
var config = userConfigProvider.Read<MyConfig>("settings.json");

// Запись
userConfigProvider.Write("settings.json", myConfig, AppUserConfigWriteTarget.AppData);
```

**Порядок поиска файлов:**
1. Текущая директория (если задана)
2. AppData (если задана)
3. UserProfile (если задана)

#### 4. Конфигурация как очередь (Reactive)

**Регистрация конфигурации как очереди:**

```csharp
.UseConfigAsQueue<MyConfig>(
  new AppQueueConsumeRequirement<AppConfig<MyConfig>>(
    config =>
    {
      // Обработка обновления конфигурации
      if (config.Status == AppConfigStatus.Ok)
      {
        UpdateService(config.Value);
      }
    }
  )
)
```

**Преимущества:**
- Автоматическая обработка обновлений конфигурации
- Интеграция с Reactive Extensions (Rx)
- Поддержка hot-reload

### Работа с секретами

**Рекомендуемый подход:**

1. **Разработка:** Используйте файлы конфигурации (не коммитьте секреты в Git)
2. **Production:** Используйте переменные окружения или секреты из внешних систем

**Пример с секретами:**

```csharp
[AppConfig(Name = "app-config.json")]
public class AppConfig
{
  public string ApiUrl { get; set; } = "";
  // Секреты НЕ хранятся в файле
}

public class SecretsConfig
{
  public string ApiKey { get; set; } = "";
  public string DatabasePassword { get; set; } = "";
}

// Загрузка секретов из переменных окружения
.UseAppEnvConfig<SecretsConfig>(ctx => new SecretsConfig
{
  ApiKey = ctx.AsString("API_KEY"),
  DatabasePassword = ctx.AsString("DB_PASSWORD")
})

// Использование
.OnStart(ctx =>
{
  var appConfig = ctx.Locate<AppConfig>();
  var secrets = ctx.Locate<SecretsConfig>();

  // Используем конфигурацию и секреты вместе
  InitializeService(appConfig.ApiUrl, secrets.ApiKey);
})
```

**Безопасность:**
- ✅ Секреты в переменных окружения
- ✅ Файлы конфигурации в `.gitignore`
- ✅ Использование `AppUserConfigProvider` для пользовательских настроек
- ❌ Не храните секреты в файлах конфигурации в репозитории

---

## 📚 Полезные ссылки

* [Руководство по установке NuGet ленты MCDis в Visual Studio](https://git.imbalanced.tech/knowledge-base/backend/-/blob/main/Guideline/nuget-tape-nexus/nuget-tape-VS-installation.md)


